using System;

namespace ConsoleApplication18
{
    class ForeachLoopMessageLength
    {
        static void Main()
        {
            string message;
            int total = 0;

            Console.WriteLine("Please enter your message: ");
            message = Console.ReadLine();

            foreach (char i in message)
            {
                total = total + 1;
                if (i = char e)

            }

            Console.WriteLine(total);
            Console.ReadLine();
        }
    }
}
